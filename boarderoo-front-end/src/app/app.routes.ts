import { Routes } from '@angular/router';
import { JoinUsComponent } from './join-us/join-us.component';

export const routes: Routes = [
  { path: '', component: JoinUsComponent } // Domyślna ścieżka
];
